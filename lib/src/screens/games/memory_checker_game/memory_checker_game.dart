import 'package:flutter/material.dart';

class MemoryCheckerGame extends StatelessWidget {
  static const route_name = "memory_checker_game";

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
